# v0.3 技术与运行优化

- 将前置判断压缩为四项必须由使用者确认的内容，其余制作参数由 AI 提供默认建议。
- 增加首次、核对和快速三种执行模式，让成熟配置减少重复询问。
- 增加 `ppt-config.md` 固定入口模板，支持不同 Agent 找到同一套个人配置。
- 增加统一的 `review-checklist.md` 模板。
- 为长期口径增加作用域、适用前提、重新核验条件和隐私清理要求。
- 增加轻量的成长指标，检查第二次同类任务是否真正减少确认和返工。
- 增加无第三方依赖的包结构与 Markdown 链接校验脚本。
