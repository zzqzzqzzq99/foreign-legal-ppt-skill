# 安装和启动

## 最简单的方式

把整个 `foreign-legal-ppt-skill` 文件夹或压缩包交给支持 Skills 的 Agent，然后发送：

> 请把这个 Skill 安装到你的个人 Skills 目录。安装后运行“设置我的 PPT 标准”，引导我生成个人配置文件。未经我同意，不要上传我的模板、客户资料或内部文件。

如果 Agent 不能自动安装，请将该文件夹复制到其个人 Skills 目录，并重新启动或刷新 Skills。不同产品的目录和安装方式可能不同，应让当前 Agent 依据其官方说明确定位置。

## 第一次使用

发送：

> 使用 `$foreign-legal-ppt` 设置我的 PPT 标准。请一次只问少量问题，并通过参考 PPT 或样张帮助我选择。

配置完成后，个人偏好会保存为独立的 `ppt-profile.md`，不会因更新 Skill 而被覆盖。

## 日常使用

发送：

> 按照我的 PPT 标准，制作一份面向中国管理层的 CFIUS 交易指南。检索并核验截至今天的官方法源；先做前置约束（对象、重点注意方面），给出 2 套汇报结构供我确认后再制作；AI 负责检索与美观，口径判断交给我。先查口径库是否已有 CFIUS 条目。

配置完成后，个人偏好保存在独立的 `ppt-profile.md`，口径积累保存在用户工作目录的 `口径库/`，均不写回公共 Skill 仓库。

## 能力边界

该 Skill 定义研究、表达和质检标准。实际生成 PPTX、联网检索、读取模板和渲染预览仍取决于当前 Agent 拥有的工具和权限。涉及真实客户资料时，先确认数据处理边界和获准使用的环境。

如果使用较轻量或速度优先的模型，建议发送：

> 使用 `$foreign-legal-ppt` 完成这项任务。请把议题地图、来源台账、大纲和 PPT 制作分阶段保存；先检查覆盖面，再开始排版。不要因当前 Agent 缺少检索或渲染能力而宣称已经完成核验。
